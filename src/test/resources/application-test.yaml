spring:
  liquibase:
    change-log: classpath:changelog-h2.sql
    enabled: true
  sql:
    init:
      mode: never
  h2:
          console:
            enabled: true
            path: /h2-console


